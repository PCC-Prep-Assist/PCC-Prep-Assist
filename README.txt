PCC Prep Assist 4.0.0 - internal GitLab edition

LOCAL USE
1. Extract the complete ZIP archive.
2. Open index.html.

GITLAB PAGES
Follow START-HIER-GITLAB.md. Upload every file and folder to the repository root.
The GitLab pipeline validates, builds and publishes the application automatically.

DEVELOPMENT
Build: npm run build
Tests and verification: npm run check

The interface CSS and JavaScript are bundled directly into index.html.
